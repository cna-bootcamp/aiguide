# AI 서비스 기획 자동화 에이전트 시스템

이 시스템은 README.md의 각 기획 단계를 자동화하는 AI 에이전트들로 구성되어 있습니다. 각 에이전트는 협업하여 완전한 서비스 기획을 생성합니다.

## 📋 목차

- [시스템 개요](#시스템-개요)
- [에이전트 목록](#에이전트-목록)
- [설치 및 설정](#설치-및-설정)
- [사용 방법](#사용-방법)
- [프로젝트 구조](#프로젝트-구조)
- [출력 결과](#출력-결과)
- [커스터마이징](#커스터마이징)

## 🎯 시스템 개요

### 아키텍처

```
┌─────────────────────────────────────────────────────┐
│              PlanningOrchestrator                    │
│           (중앙 조정 및 워크플로우 관리)                │
└─────────────────────────────────────────────────────┘
                        │
        ┌───────────────┼───────────────┐
        │               │               │
┌───────▼──────┐ ┌─────▼─────┐ ┌──────▼──────┐
│ Stage 1      │ │ Stage 2   │ │ Stage 3     │
│ 주제/고객    │ │ 문제발견  │ │ 솔루션탐색  │
│              │ │           │ │             │
│ • MVP Agent  │ │ • Market  │ │ • Ideation │
│ • Customer   │ │ • CX      │ │ • Selection│
│              │ │ • Journey │ │            │
└──────────────┘ └───────────┘ └─────────────┘

┌──────────────┐ ┌───────────┐ ┌─────────────┐
│ Stage 4      │ │ Stage 5   │ │ Stage 6     │
│ 비즈니스     │ │ 제품설계  │ │ 프로토타입  │
│              │ │           │ │             │
│ • Business   │ │ • Event   │ │ • Prototype│
│   Model      │ │ • Stories │ │             │
│              │ │ • UI/UX   │ │             │
└──────────────┘ └───────────┘ └─────────────┘

                        │
        ┌───────────────┼───────────────┐
        │               │               │
┌───────▼──────┐ ┌─────▼─────┐ ┌──────▼──────┐
│ SharedState  │ │  Outputs  │ │   Reports   │
│ (상태 관리)  │ │ (결과저장)│ │  (리포트)   │
└──────────────┘ └───────────┘ └─────────────┘
```

### 워크플로우

1. **Stage 1: 주제 및 고객 정의**
   - MVP 주제 선정
   - 대상 고객 정의 (JTBD)

2. **Stage 2: 문제 발견**
   - 시장 조사
   - 고객 경험 조사 (인터뷰/관찰/체험)
   - User Journey Map
   - 문제 가설 정의

3. **Stage 3: 솔루션 탐색**
   - 아이디어 생성 (브레인스토밍)
   - 솔루션 평가 및 선정 (ICE 스코어링)

4. **Stage 4: 비즈니스 모델**
   - Lean Canvas 작성
   - 수익 모델 설계

5. **Stage 5: 제품 설계**
   - 이벤트 스토밍 (시퀀스 다이어그램)
   - 유저스토리 작성 (INVEST)
   - UI/UX 설계서

6. **Stage 6: 프로토타입**
   - 프로토타입 개발 가이드
   - 기술 스택 제안

## 🤖 에이전트 목록

### 1. MVP Agent (MVP 주제 정의)
**역할**: 비즈니스 도메인을 결정하고 MVP 주제를 선정합니다.

**입력**:
- 사용자의 아이디어나 관심사 (선택사항)

**출력**:
- MVP 주제
- 주제 선정 이유
- 시장 잠재력 분석
- 주요 특징

### 2. Customer Agent (대상 고객 정의)
**역할**: JTBD(Jobs To Be Done) 형식으로 대상 고객을 정의합니다.

**입력**:
- MVP 주제

**출력**:
- 대상 고객 유형 (사용자군 + 완수하려는 과업)
- 사용자 세그먼트
- Pain Points
- 고객의 기대와 욕구

### 3. Market Research Agent (시장 조사)
**역할**: 심층 시장 분석을 수행합니다.

**입력**:
- MVP 주제
- 대상 고객

**출력**:
- 시장 규모 및 성장성
- 경쟁 환경 분석
- 고객 트렌드
- SWOT 분석
- 시장 진입 전략

### 4. Customer Experience Agent (고객 경험 조사)
**역할**: 고객 인터뷰, 관찰, 체험을 통해 실제 고객 경험 데이터를 수집합니다.

**입력**:
- MVP 주제
- 대상 고객
- 시장 조사 결과

**출력**:
- 고객 인터뷰 결과 (질문/응답, 인사이트)
- 고객 관찰 결과 (행동 패턴, Pain Points)
- 고객 체험 결과 (만족도, 사용 후기)
- 종합 인사이트 및 개선 기회 영역

### 5. Journey Map Agent (고객 여정 지도)
**역할**: 고객의 전체 여정을 시각화하고 분석합니다.

**입력**:
- MVP 주제
- 대상 고객
- 인터뷰/관찰 결과 (있는 경우)

**출력**:
- 페르소나
- 여정 단계별 분석 (행동, 사고, 감정, 터치포인트)
- Pain Points & Gain Points
- 핵심 인사이트
- 기회 영역

### 5. Problem Hypothesis Agent (문제 가설)
**역할**: 고객의 핵심 문제를 검증 가능한 가설로 정의합니다.

**입력**:
- Journey Map
- 대상 고객
- 인터뷰 결과

**출력**:
- 주 문제 가설
- 근본 원인 분석 (5 Whys)
- 문제의 영향 범위
- 현재 대안 및 한계
- 검증 가능한 가설 진술
- 검증 계획

### 7. Ideation Agent (아이디어 생성)
**역할**: 문제 해결을 위한 창의적인 아이디어를 생성합니다.

**입력**:
- 문제 가설
- 방향성 (있는 경우)

**출력**:
- 최소 10개 이상의 솔루션 아이디어
- 아이디어 카테고리 분류
- 와일드 카드 아이디어
- 아이디어 조합
- 초기 필터링 기준

### 8. Solution Selection Agent (솔루션 선정)
**역할**: 아이디어를 체계적으로 평가하고 최적의 솔루션을 선정합니다.

**입력**:
- 아이디에이션 결과
- 문제 가설

**출력**:
- ICE/RICE 스코어링 결과
- 아이디어 평가표
- 상위 3개 아이디어 분석
- 최종 선정 솔루션
- 핵심 기능 정의
- MVP 로드맵
- 성공 지표 (KPI)

### 9. Business Model Agent (비즈니스 모델)
**역할**: Lean Canvas 기반 비즈니스 모델을 설계합니다.

**입력**:
- 선정된 솔루션
- 대상 고객
- 문제 가설

**출력**:
- Lean Canvas 9개 블록
- Business Model Canvas
- 경쟁 분석 및 포지셔닝
- Go-to-Market 전략
- 재무 계획 (3년)

### 10. Event Storming Agent (이벤트 스토밍)
**역할**: 이벤트 스토밍 기법을 활용하여 시스템의 이벤트 흐름과 시퀀스 다이어그램을 작성합니다.

**입력**:
- 선정된 솔루션
- 대상 고객
- Journey Map
- 비즈니스 모델

**출력**:
- 도메인 이벤트 식별
- 커맨드 및 애그리게이트 정의
- 시퀀스 다이어그램 (Mermaid 형식)
- 바운디드 컨텍스트
- User Story 작성을 위한 권장사항

### 11. User Story Agent (유저스토리)
**역할**: 사용자 관점의 요구사항을 체계적으로 작성합니다.

**입력**:
- 선정된 솔루션
- 대상 고객
- 이벤트 스토밍 결과

**출력**:
- Epic 별 분류
- 20개 이상의 User Stories (INVEST 원칙)
- Acceptance Criteria
- 우선순위 매트릭스 (MoSCoW)
- 스프린트 계획
- Non-Functional Requirements
- Definition of Done

### 12. UI/UX Agent (UI/UX 설계)
**역할**: 사용자 경험과 인터페이스를 설계합니다.

**입력**:
- 유저스토리
- 선정된 솔루션

**출력**:
- 디자인 원칙
- 정보 구조 (IA)
- 화면 흐름도 (User Flow)
- 와이어프레임 (주요 화면)
- 컴포넌트 라이브러리
- 스타일 가이드 (색상, 타이포그래피, 간격)
- 반응형 디자인
- 접근성 가이드

### 13. Prototype Agent (프로토타입)
**역할**: 프로토타입 개발 계획과 구현 가이드를 제공합니다.

**입력**:
- UI/UX 설계
- 유저스토리

**출력**:
- 기술 스택 제안
- 프로젝트 구조
- 데이터 모델 및 스키마
- API 설계
- 구현 로드맵 (2-3주)
- 핵심 기능 구현 가이드 (코드 예시)
- 보안 및 성능 최적화
- 배포 가이드
- 테스팅 전략

## 🚀 설치 및 설정

### 1. 사전 요구사항

- Python 3.8 이상
- pip (Python 패키지 관리자)
- Anthropic API Key

### 2. 설치

```bash
# 1. 저장소 클론 (이미 되어 있음)
cd aiguide

# 2. 의존성 설치
pip install -r requirements.txt
```

### 3. API Key 설정

```bash
# .env 파일 생성
cp .env.example .env

# .env 파일 편집
# ANTHROPIC_API_KEY=your-api-key-here
```

또는 환경변수로 설정:

```bash
export ANTHROPIC_API_KEY='your-api-key-here'
```

API Key는 [Anthropic Console](https://console.anthropic.com/)에서 발급받을 수 있습니다.

## 💻 사용 방법

### 1. 전체 기획 프로세스 실행

```bash
# 기본 실행
python main.py --project my_service --full

# MVP 주제를 지정하여 실행
python main.py --project food_delivery --full --input "음식 배달 서비스"

# 또 다른 예시
python main.py --project travel_planner --full --input "AI 기반 여행 계획"
```

### 2. 특정 에이전트만 실행

```bash
# MVP 주제 정의만 실행
python main.py --project my_service --agent mvp --input "구독 관리 서비스"

# 대상 고객 정의 실행 (이전 상태 로드)
python main.py --project my_service --agent customer

# UI/UX 설계만 실행
python main.py --project my_service --agent uiux
```

### 3. 대화형 모드

```bash
python main.py --interactive
```

대화형 모드에서는:
1. 프로젝트 이름 입력
2. 실행 모드 선택 (전체/단일 에이전트)
3. 필요한 추가 정보 입력

### 4. 에이전트 목록 보기

```bash
python main.py --list
```

### 5. 도움말 보기

```bash
python main.py --help
```

## 📁 프로젝트 구조

```
aiguide/
├── agents/                      # 에이전트 모듈
│   ├── __init__.py
│   ├── base_agent.py           # 베이스 에이전트 클래스
│   ├── orchestrator.py         # 오케스트레이터
│   ├── mvp_agent.py            # MVP 주제 정의
│   ├── customer_agent.py       # 대상 고객 정의
│   ├── market_research_agent.py # 시장 조사
│   ├── customer_experience_agent.py # 고객 경험 조사
│   ├── journey_map_agent.py    # User Journey Map
│   ├── problem_hypothesis_agent.py # 문제 가설
│   ├── ideation_agent.py       # 아이디어 생성
│   ├── solution_selection_agent.py # 솔루션 선정
│   ├── business_model_agent.py # 비즈니스 모델
│   ├── event_storming_agent.py # 이벤트 스토밍
│   ├── user_story_agent.py     # 유저스토리
│   ├── uiux_agent.py           # UI/UX 설계
│   └── prototype_agent.py      # 프로토타입
│
├── shared/                      # 공유 모듈
│   ├── __init__.py
│   ├── state.py                # 상태 관리
│   └── utils.py                # 유틸리티 함수
│
├── config/                      # 설정 파일 (선택사항)
│   └── agent_config.yaml
│
├── outputs/                     # 출력 결과 (자동 생성)
│   └── [project_name]/
│       ├── 00_summary_report.md
│       ├── 01_mvp_topic.md
│       ├── 02_target_customer.md
│       ├── 03_market_research.md
│       ├── 04_customer_experience.md
│       ├── 05_journey_map.md
│       ├── 06_problem_hypothesis.md
│       ├── 07_ideation.md
│       ├── 08_solution_selection.md
│       ├── 09_business_model.md
│       ├── 10_event_storming.md
│       ├── 11_user_stories.md
│       ├── 12_uiux_design.md
│       ├── 13_prototype_guide.md
│       └── [project_name]_state.json
│
├── examples/                    # 예제 출력물
│
├── main.py                      # 메인 실행 스크립트
├── requirements.txt             # Python 의존성
├── .env.example                 # 환경변수 예시
├── README.md                    # 기획 가이드 (원본)
└── README_AGENTS.md             # 에이전트 시스템 설명서 (이 파일)
```

## 📊 출력 결과

### 생성되는 파일

각 에이전트는 Markdown 형식의 결과물을 생성합니다:

| 파일명 | 설명 | 에이전트 |
|--------|------|----------|
| `00_summary_report.md` | 전체 결과 요약 | Orchestrator |
| `01_mvp_topic.md` | MVP 주제 정의서 | MVP Agent |
| `02_target_customer.md` | 대상 고객 정의서 | Customer Agent |
| `03_market_research.md` | 시장 조사 보고서 | Market Research Agent |
| `04_customer_experience.md` | 고객 경험 조사 보고서 | Customer Experience Agent |
| `05_journey_map.md` | User Journey Map | Journey Map Agent |
| `06_problem_hypothesis.md` | 문제 가설 정의서 | Problem Hypothesis Agent |
| `07_ideation.md` | 아이디어 목록 | Ideation Agent |
| `08_solution_selection.md` | 솔루션 평가 및 선정서 | Solution Selection Agent |
| `09_business_model.md` | 비즈니스 모델 (Lean Canvas) | Business Model Agent |
| `10_event_storming.md` | 이벤트 스토밍 및 시퀀스 다이어그램 | Event Storming Agent |
| `11_user_stories.md` | 유저스토리 | User Story Agent |
| `12_uiux_design.md` | UI/UX 설계서 | UI/UX Agent |
| `13_prototype_guide.md` | 프로토타입 개발 가이드 | Prototype Agent |
| `{project}_state.json` | 전체 상태 (JSON) | Orchestrator |

### 상태 파일 (state.json)

모든 에이전트의 결과가 저장되어 있어:
- 중간에 중단된 작업을 이어서 진행 가능
- 특정 단계만 다시 실행 가능
- 다른 도구에서 데이터 활용 가능

## 🎨 커스터마이징

### 1. 새로운 에이전트 추가

```python
# agents/my_custom_agent.py
from .base_agent import BaseAgent
from typing import Dict, Any

class MyCustomAgent(BaseAgent):
    def __init__(self):
        super().__init__(
            name="커스텀 에이전트",
            description="나만의 에이전트 설명"
        )

    def get_prompt_template(self, context: Dict[str, Any]) -> str:
        return """
        당신은 [역할] 전문가입니다.
        [작업 설명]
        """

    async def execute(self, context: Dict[str, Any]) -> Dict[str, Any]:
        try:
            prompt = self.get_prompt_template(context)
            response = await self.call_claude(prompt)

            result = self.format_output(
                content=response,
                metadata={"custom_field": "value"}
            )

            return await self.post_execute(result)

        except Exception as e:
            return self.format_error(f"Error: {str(e)}")
```

### 2. 워크플로우 수정

`agents/orchestrator.py`의 `workflow` 속성을 수정하여 단계를 추가/제거/순서변경할 수 있습니다.

### 3. 프롬프트 커스터마이징

각 에이전트의 `get_prompt_template()` 메서드를 수정하여 프롬프트를 변경할 수 있습니다.

### 4. 출력 형식 변경

`shared/utils.py`의 함수들을 수정하여 출력 형식을 변경할 수 있습니다.

## 🔧 문제 해결

### API Key 오류

```
⚠️ Warning: ANTHROPIC_API_KEY not found!
```

**해결방법**:
1. `.env` 파일에 API Key 설정
2. 또는 환경변수로 설정: `export ANTHROPIC_API_KEY='your-key'`

### 모듈 import 오류

```
ModuleNotFoundError: No module named 'anthropic'
```

**해결방법**:
```bash
pip install -r requirements.txt
```

### 출력 디렉토리 권한 오류

**해결방법**:
```bash
mkdir -p outputs
chmod 755 outputs
```

## 📚 참고 자료

- [Anthropic Claude API 문서](https://docs.anthropic.com/)
- [Lean Canvas](https://leanstack.com/lean-canvas)
- [User Story Mapping](https://www.jpattonassociates.com/user-story-mapping/)
- [Jobs To Be Done](https://jobs-to-be-done.com/)
- [ICE Scoring](https://www.intercom.com/blog/rice-simple-prioritization-for-product-managers/)

## 🤝 기여

버그 리포트, 기능 제안, Pull Request는 언제나 환영합니다!

## 📝 라이선스

이 프로젝트는 원본 가이드(README.md)와 동일한 라이선스를 따릅니다:
[Creative Commons Attribution-NonCommercial-ShareAlike 4.0 International License](http://creativecommons.org/licenses/by-nc-sa/4.0/)

---

**만든이**: 이해경 (hiondal@gmail.com), 유니콘주식회사
**에이전트 시스템**: Claude Code를 이용한 자동화
